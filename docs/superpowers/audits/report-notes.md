- [태스크3][Low] IllegalStateException 핸들러가 전역 범위 — RedisOAuth2AuthorizedClientRepository의 영어 메시지 IllegalStateException이 이론상 4xx로 노출 가능(현재는 필터 체인 단계라 미도달). 다음 IllegalStateException 추가 시 주의.
- [태스크4][Medium] Bean Validation 메시지 JVM 로케일 의존 — 프로덕션 Dockerfile(alpine)에 로케일 미설정이라 배포 환경에서 영어 노출 가능성 있었음. translateValidationMessage에 Size 패턴 변환 추가로 해소(픽스 완료). 다른 표준 제약(@Email 등) 추가 시 같은 함정 주의.
- [태스크5][Medium] PATCH /ideas/{id}가 parentIdeaId를 필드 생략과 명시적 null을 구분 못 함 — `Idea.update()`(entity/Idea.java:69-75)는 title·content·pinned·category는 "null이면 유지" 패턴인데 parentIdea만 `this.parentIdea = parentIdea;`로 무조건 덮어씀. `IdeaServiceImpl.updateIdea`(89번째 줄)가 요청의 parentIdeaId==null이면 findParentIdea도 null을 반환하므로, parentIdeaId를 생략한 부분 수정(예: content만 변경)이 조용히 상위 아이디어 연결을 끊는다. 현재 FE(IdeaDumpPage.jsx:310-315)는 매 PATCH마다 parentIdeaId를 항상 함께 보내 우연히 미발현. UUID null이 "루트로 이동"과 "필드 미제공" 두 의미를 겸하는 API 계약 자체의 결함이라 단순 null 체크로는 못 고치고 Optional/JsonNullable 같은 존재유무 래퍼 도입이 필요 — 설계 변경 필요, 이번 태스크에서 픽스하지 않음. 제안: parentIdeaId를 `Optional<UUID>`나 별도 `clearParent` 플래그로 분리.
- [태스크5][Low] POST /ideas/ai-extract/confirm의 `IdeaExtractConfirmRequest.ideas`가 `@NotNull`만 있고 `@NotEmpty`가 없어 빈 배열(`{"ideas":[]}`)이 400이 아닌 201(빈 배열 생성)로 통과하던 문제 — Task의 `SplitRequest.subtasks`(`@NotEmpty`) 패턴과 불일치. 같은 태스크에서 `@NotEmpty`로 즉시 픽스(dto/IdeaExtractConfirmRequest.java).
