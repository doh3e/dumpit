- [태스크3][Low] IllegalStateException 핸들러가 전역 범위 — RedisOAuth2AuthorizedClientRepository의 영어 메시지 IllegalStateException이 이론상 4xx로 노출 가능(현재는 필터 체인 단계라 미도달). 다음 IllegalStateException 추가 시 주의.
- [태스크4][Medium] Bean Validation 메시지 JVM 로케일 의존 — 프로덕션 Dockerfile(alpine)에 로케일 미설정이라 배포 환경에서 영어 노출 가능성 있었음. translateValidationMessage에 Size 패턴 변환 추가로 해소(픽스 완료). 다른 표준 제약(@Email 등) 추가 시 같은 함정 주의.
- [태스크5][Medium] PATCH /ideas/{id}가 parentIdeaId를 필드 생략과 명시적 null을 구분 못 함 — `Idea.update()`(entity/Idea.java:69-75)는 title·content·pinned·category는 "null이면 유지" 패턴인데 parentIdea만 `this.parentIdea = parentIdea;`로 무조건 덮어씀. `IdeaServiceImpl.updateIdea`(89번째 줄)가 요청의 parentIdeaId==null이면 findParentIdea도 null을 반환하므로, parentIdeaId를 생략한 부분 수정(예: content만 변경)이 조용히 상위 아이디어 연결을 끊는다. 현재 FE(IdeaDumpPage.jsx:310-315)는 매 PATCH마다 parentIdeaId를 항상 함께 보내 우연히 미발현. UUID null이 "루트로 이동"과 "필드 미제공" 두 의미를 겸하는 API 계약 자체의 결함이라 단순 null 체크로는 못 고치고 Optional/JsonNullable 같은 존재유무 래퍼 도입이 필요 — 설계 변경 필요, 이번 태스크에서 픽스하지 않음. 제안: parentIdeaId를 `Optional<UUID>`나 별도 `clearParent` 플래그로 분리.
- [태스크5][Low] POST /ideas/ai-extract/confirm의 `IdeaExtractConfirmRequest.ideas`가 `@NotNull`만 있고 `@NotEmpty`가 없어 빈 배열(`{"ideas":[]}`)이 400이 아닌 201(빈 배열 생성)로 통과하던 문제 — Task의 `SplitRequest.subtasks`(`@NotEmpty`) 패턴과 불일치. 같은 태스크에서 `@NotEmpty`로 즉시 픽스(dto/IdeaExtractConfirmRequest.java).
- [태스크5][Medium] AI 사용량 한도가 Redis 장애 시 fail-open — AiUsageServiceImpl.consume()이 DataAccessException 시 REDIS_UNAVAILABLE로 기록만 하고 무제한 허용. 프로덕션 Redis 장애 시 AI 한도 뚫림(비용 노출). 제안: fail-closed 전환 또는 Sentry 알림+DB 폴백 카운터 검토.
- [태스크6][Low] `CalendarController.getEvents`의 `client == null` 분기(69-74번째 줄, "빈 목록 200")가 google 클라이언트 등록(`authorization_code` grant)에서는 도달 불가능한 죽은 코드로 확인됨 — `DefaultOAuth2AuthorizedClientManager.authorize()`는 저장된 인가 클라이언트가 없으면 null을 반환하는 대신 항상 `ClientAuthorizationRequiredException`(OAuth2AuthorizationException 하위타입)을 던지므로(`AuthorizationCodeOAuth2AuthorizedClientProvider.authorize()` 소스 확인, spring-security-oauth2-client 6.4.2), 미연결 상태는 실제로는 바로 위 `catch (OAuth2AuthorizationException ex)` 분기를 타 403 GOOGLE_CALENDAR_RECONNECT_REQUIRED로 응답한다. 향후 다른 grant type을 추가하지 않는 한 이 방어 코드는 실행되지 않는다 — 제거 여부는 설계 판단이 필요해 이번 태스크에서 픽스하지 않음(테스트로 명시적 죽은코드임을 주석 처리만 함).
- [태스크6][테스트 인프라] spring-security-test의 `oauth2Login()`(=`ApiIntegrationTestBase.asUser()`)은 요청마다 리플렉션으로 실제 `DefaultOAuth2AuthorizedClientManager` 빈의 `authorizedClientRepository` 필드를 "test" 등록ID·scope=[read] 고정 토큰을 돌려주는 내부 테스트 전용 리포지토리로 바꿔치기한다(`OAuth2AuthorizedClientArgumentResolver`가 컨텍스트에 있기만 하면 항상 발동, `SecurityMockMvcRequestPostProcessors$OAuth2ClientRequestPostProcessor` 바이트코드로 확인). 그 결과 `@MockBean OAuth2AuthorizedClientRepository`를 아무리 `given()`으로 스텁해도 `CalendarController`처럼 `OAuth2AuthorizedClientManager.authorize()`를 직접 호출하는 컨트롤러는 항상 그 고정값을 받는다 — 스코프별 시나리오 테스트가 불가능했다. `DashboardCalendarApiTest`에서는 `OAuth2AuthorizedClientManager` 자체를 `@MockBean`으로 교체해 우회함(인스턴스가 `DefaultOAuth2AuthorizedClientManager`가 아니게 되어 훅의 `instanceof` 체크를 벗어남). `@RegisteredOAuth2AuthorizedClient` 파라미터를 쓰는 다른 컨트롤러가 생기면 같은 함정을 다시 만날 수 있음 — 참고용으로 기록.
- [태스크7][Low] `GlobalExceptionHandler.koreanBadRequestMessage`의 라우틴 관련 5개 switch case(`"Weekly routines need at least one day between 1 and 7."` 등 영문 원문 → 한글 매핑, 94~98번째 줄)가 죽은 코드로 확인됨 — `RoutineServiceImpl.validateRepeatRule`/`validateDateRange`는 애초에 `BadRequestException`(ApiException 하위타입, `handleApiException`이 처리)에 한글 메시지를 직접 담아 던지므로 `IllegalArgumentException`/`IllegalStateException`을 거쳐야 발동하는 `koreanBadRequestMessage` 번역 경로를 아예 타지 않는다(코드베이스 전체에 해당 영문 원문을 `IllegalArgumentException`/`IllegalStateException`으로 던지는 곳 없음, grep으로 확인). 동작 자체는 이미 올바른 한글을 반환해 버그는 아니지만, 향후 리팩터링 때 "실제로 쓰이는 매핑"으로 오인하기 쉬운 죽은 분기라 정리 대상으로 기록만 함(이번 태스크에서는 테스트만 추가, switch 정리는 범위 밖).
- [태스크8][Medium] `NoticeController`의 `resolveUser()` null 방어 분기(64~70번째 줄, 호출부 31~47)는 평상시 도달 불가능한 죽은 코드이지만, 로그인 세션 유지 중 계정이 밴/탈퇴된 예외 케이스에서는 실제로 실행된다 — Spring Security의 OAuth2User principal은 로그인 시점에 세션에 캐시되고 이후 요청마다 DB를 재조회하지 않으므로(`CustomOAuth2UserServiceImpl.loadUser`는 로그인 흐름에서만 호출), 밴된 유저도 만료 전 세션으로는 계속 인증된 상태를 유지한 채 각 컨트롤러의 개별 `isActive()` 체크에만 걸린다. 이때 NoticeController는 `ResponseEntity.status(401).build()`로 본문 없는 응답을 반환해, 앱 전역의 "모든 오류 응답은 error 필드를 가진 한글 JSON" 계약(`assertKoreanError`가 검증하는 것과 동일)을 깬다. 다만 `AuthController.me()`(26~34번째 줄)도 동일하게 본문 없는 401/403을 반환하는 기존 패턴이라, Notice만 개별 픽스하면 컨트롤러 간 불일치가 오히려 커짐 — 이번 태스크 범위 밖(케이스 표에 없는 시나리오)이라 픽스하지 않음. 제안: 밴/탈퇴된 세션을 감지해 일관된 한글 오류 바디를 내려주는 공통 필터/인터셉터 도입 검토.
- [태스크10][High] 밴된 유저가 세션이 살아있는 동안 `TaskController`의 모든 엔드포인트를 계속 정상 사용할 수 있음(실증: `AdminApiTest.밴된유저_세션유지중_GET_tasks_차단여부_실증`, 관리자가 userB를 밴 처리한 뒤 같은 세션으로 `GET /tasks` 호출 → 200 OK) — `TaskServiceImpl.findUser()`(TaskServiceImpl.java:357-360)는 `userRepository.findByEmail(email)`만 하고 `AuthController.me()`/`ProfileController.resolveUser()`처럼 `.filter(User::isActive)`를 걸지 않는다. 태스크8에서 기록한 "밴/탈퇴 세션의 본문 없는 401" 이슈와 같은 근본 원인(로그인 시점 캐시된 OAuth2User 세션 + 컨트롤러/서비스별로 제각각인 `isActive()` 체크)의 다른 얼굴 — 이번엔 401/403조차 없이 그냥 정상 200이 나온다는 점에서 더 심각함(TaskController 외 `isActive()` 체크가 없는 다른 컨트롤러도 동일 노출 가능성, 전수 확인은 범위 밖). 개별 서비스마다 `isActive()` 체크를 흩뿌리는 방식은 누락되기 쉬우므로(TaskServiceImpl이 실제로 누락된 사례), 인증 성공 이후 매 요청마다 유저 상태를 검증하는 공통 필터/인터셉터 도입이 필요 — 설계 변경 사안이라 이번 태스크에서 픽스하지 않음. 제안: OAuth2 인증 필터 체인 뒤에 "유저 상태(BANNED/WITHDRAWN)면 즉시 401 한글 JSON" 공통 게이트 추가, 태스크8 Medium 노트와 함께 해소.
- [태스크12][Low] SQL 전수 로그(268개 테스트, 통계: insert 850·select 370·update 51·delete 1) 스캔 결과 **추가 N+1 없음** — 코드 픽스 대상 0건. 근거: (1) 전체 SELECT 템플릿을 정규화해 빈도 집계한 결과, 반복 최다 항목(121회 `select ... from users where email=?` 등)은 268개 테스트 각각이 독립적으로 로그인·리소스 1건을 조회하는 정상 패턴이었고 "요청 1회당 같은 SELECT 반복"에 해당하는 패턴은 없었음. (2) 서비스/컨트롤러 전체의 `for`/`forEach` 루프를 grep해 리포지토리 호출을 감싸는 곳을 전수 확인 — `AccountServiceImpl.withdraw`(브레인덤프·문의 익명화), `IdeaServiceImpl.saveNodeRecursive`(AI 추출 확정), `TaskServiceImpl`(분할 생성), `BrainDumpServiceImpl`(확정) 전부 "유저가 입력한 항목 수만큼 쓰기"로 항목 수에 비례하는 것이 의도된 동작이지 테이블 전체 크기에 비례하는 N+1이 아님. `RoutineServiceImpl.generateDueRoutines`/`backfillMissingNextRunAt`(스케줄러)도 루틴별 개별 판단이 불가피한 배치 잡으로 동일 성격(현재 routines 0행, 저빈도 백그라운드 잡이라 우선순위 낮음, 액션 아이템 아님). (3) 엔티티 전수 확인 결과 `@OneToMany`가 코드베이스에 전혀 없고(모든 연관관계가 `@ManyToOne(LAZY)`뿐) 이미 태스크4/5에서 "FK ID만 응답에 사용, 프록시 미초기화"로 무혐의 확인된 패턴과 일치 — 컬렉션 사이드 N+1의 구조적 위험이 없음. (4) 목록형 API 7종의 쿼리 카운트 회귀 테스트(GET /tasks·ideas·routines·notices·dashboard/planning 각 2, shop/catalog 3, admin/users 6)는 전부 실측값+여유 2로 이미 타이트하게 고정되어 있어 상한을 더 좁힐 여지 없음. 결론: 이번 사이클의 유일한 N+1(태스크10 admin/users)은 이미 해소되었고 이번 태스크에서 신규 발견 없음.
- [태스크12][Medium] `tasks` 테이블에 PK(`task_id`) 외 인덱스가 전무함 — Postgres는 FK 컬럼에 자동 인덱스를 만들지 않으므로 `user_id`도 무인덱스. `findByUserOrderByPriority`/`findByUserWithRecentDoneOrderByPriority`/`findDeadlineNudges`/`findOverdueTasks`/`findDeadlineIndexCandidates`/`countDoneByCategory`/`findCompletedAtSince` 7개 쿼리 전부 `WHERE user_id = ? AND deleted_at IS NULL [AND status ...] [AND deadline ...]` 형태로 이 두 컬럼을 공통 술어로 씀. 로컬 `dumpit` DB(실데이터 9행)에서 `EXPLAIN ANALYZE` 확인 결과 현재는 전부 `Seq Scan`(cost 10 내외, 9행 전체 스캔 후 필터, 실행시간 0.03~0.09ms)이라 문제 없음 — 개인 서비스 규모(수백 행)에서도 체감 차이는 없을 것. 다만 유저 수·태스크 수가 늘어나면(특히 `countActiveGroupedByUser`처럼 유저 필터 없이 전체를 훑는 admin 집계 제외 나머지 6개는 요청마다 `user_id` 필터가 걸리므로) 테이블 전체 크기에 비례해 스캔 비용이 커지는 전형적 패턴. **제안(스키마 변경 — 픽스 보류)**: `CREATE INDEX idx_tasks_user_id_deleted_at ON tasks (user_id, deleted_at);` — 위 7개 쿼리 모두의 공통 접두 술어(`user_id`, `deleted_at IS NULL`)를 인덱스 스캔으로 좁혀준 뒤 나머지(`status`/`deadline`/`completed_at`)는 이미 좁혀진 소량 행 집합에서 인메모리 필터링하면 충분 — 컬럼 2개짜리 단일 인덱스로 7개 쿼리를 모두 커버하는 비용 대비 효율이 가장 높은 선택.
- [태스크12][Low] `AdminStatsController.today()`가 매 조회마다 `users`/`tasks`/`routines`/`brain_dumps`/`ai_usage_logs` 5개 테이블에 `COUNT(*) WHERE created_at >= 오늘0시` (ai_usage_logs는 `SUM(cost) WHERE created_at >= ? AND allowed = true` 추가) 쿼리를 날림 — 5개 테이블 전부 `created_at` 인덱스 없음. `EXPLAIN ANALYZE`로 tasks·ai_usage_logs 두 곳 확인, 둘 다 `Seq Scan`(9행·12행, 0.02~0.03ms)이라 현재 무해. admin 전용·저빈도(대시보드 열 때만) 엔드포인트라 우선순위는 Low. **제안(스키마 변경 — 픽스 보류)**: 데이터가 커지면 `CREATE INDEX idx_tasks_created_at ON tasks (created_at); CREATE INDEX idx_ai_usage_logs_created_at ON ai_usage_logs (created_at); CREATE INDEX idx_routines_created_at ON routines (created_at); CREATE INDEX idx_brain_dumps_created_at ON brain_dumps (created_at); CREATE INDEX idx_users_created_at ON users (created_at);` (5개 전부 같은 패턴이라 일괄 적용 권장, 실제 적용은 트래픽·행 수 증가 관찰 후 판단).
- [태스크12][Low] `LogRetentionScheduler`(`ActivityLogRepository.deleteWithdrawnUserLogsBefore`/`AiUsageLogRepository.deleteWithdrawnUserLogsBefore`)가 `activity_logs`/`ai_usage_logs`를 `users.withdrawn_at < cutoff` 조건으로 JOIN 삭제 — `users.withdrawn_at`에 인덱스 없음(현재 users 1행이라 무해, `EXPLAIN`으로 `activity_logs` JOIN `users` 확인: `Hash Join`, 0.04ms). 탈퇴 유저 수가 늘어나면 매 스케줄러 실행마다 `users` 전체 스캔 비용 증가. **제안(스키마 변경 — 픽스 보류)**: `CREATE INDEX idx_users_withdrawn_at ON users (withdrawn_at) WHERE withdrawn_at IS NOT NULL;`(부분 인덱스 — 탈퇴 유저만 소수일 것이므로 전체 인덱스보다 효율적).
- [태스크12][정보] `notice_reads` NOT EXISTS 서브쿼리(`NoticeReadRepository.findUnreadPublished`)는 **이미 최적화됨** — `EXPLAIN ANALYZE` 확인 결과 기존 유니크 제약 `ukfbaaj5bgwawlt6xdf6wgcbi6t (notice_id, user_id)`을 `Index Only Scan`으로 활용해 `Nested Loop Anti Join`을 구성 중(Heap Fetches: 0). 추가 조치 불필요, 기록만 함.
- [태스크12][참고] 위와 같은 이유로 `ideas.user_id`/`routines.user_id`/`brain_dumps.user_id`/`notices.author_id`/`inquiries.user_id`/`activity_logs.user_id` 등 나머지 도메인 테이블의 FK 컬럼도 전부 무인덱스(스키마 전수 확인, `\di` 대조) — 현재 전부 0~수십 행이라 우선순위는 Low. 향후 이 서비스가 성장해 인덱스를 추가하는 시점이 오면, tasks와 동일한 `(user_id[, deleted_at])` 패턴을 모든 도메인 테이블에 일괄 적용하는 편이 테이블별로 따로 판단하는 것보다 일관적이고 누락이 적음.
