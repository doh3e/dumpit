# 코인샵(상점) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 코인으로 테마(6슬롯)·스티커(4종)를 구매/장착하는 상점을 백엔드·프론트 전체에 구현한다.

**Architecture:** 카탈로그는 백엔드 코드 상수(`ShopCatalog` 서비스가 유일한 접근점), DB에는 구매(`user_purchases.item_code`)·장착(`user_equipments`)·스티커 부착(`tasks/ideas.sticker_code`)만 기록. 프론트는 `<html>`의 `data-skin-*` 속성 + CSS 변수 오버라이드로 색상 테마를, 스프라이트 레지스트리로 행성/완료/정거장을 적용한다. 스펙: `docs/superpowers/specs/2026-07-12-coin-shop-design.md`.

**Tech Stack:** Spring Boot 3 (Java 21) + Flyway + PostgreSQL / React(Vite) + Tailwind(CSS 변수 토큰).

## Global Constraints

- 브랜치: `dev`에서 `feature/coin-shop` 분기. 태스크마다 커밋.
- gradle 실행(반드시 JDK21): `cd backend && JAVA_HOME="C:/Users/o_o91/AppData/Roaming/Code/User/globalStorage/pleiades.java-extension-pack-jdk/java/21" ./gradlew test`
- **codex 서브에이전트는 이 머신에서 gradle/npm 실행 불가** — 빌드·테스트·lint 검증은 컨트롤러(오케스트레이터)가 대행한다. 서브에이전트는 코드 작성까지만.
- 커밋 컨벤션: `Feat:/Fix:/Refactor:/Chore:/Docs:` + 한국어 요약 + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` (Codex 구현분은 `Co-Authored-By: Codex <noreply@openai.com>` 추가).
- 에셋은 **CC0 라이선스만**. 출처는 `docs/ATTRIBUTIONS.md`에 기록.
- UI 카피는 기존 ~해요체("코인이 부족합니다" 등 백엔드 메시지는 기존 패턴 그대로).
- Task 5(변수 분리)는 **기본 테마 외관 불변**이 합격 기준.
- API 에러: 존재하지 않는 코드/미보유/잔액 부족 등은 `BadRequestException`(400, 한국어 메시지) — `com.dumpit.exception.BadRequestException` 사용.

## 카탈로그 확정본 (전 태스크 공통 참조)

| code | slot | tier | 이름 | 가격 |
|---|---|---|---|---|
| bg.ocean | BACKGROUND | COLOR | 오션 | 200 |
| bg.lavender | BACKGROUND | COLOR | 라벤더 | 200 |
| bg.sprout | BACKGROUND | CONCEPT | 새싹 정원 | 600 |
| chrome.mint | CHROME | COLOR | 민트 | 150 |
| chrome.rose | CHROME | COLOR | 로즈 | 150 |
| chrome.wood | CHROME | CONCEPT | 원목 서재 | 400 |
| pomo.teal | POMODORO | COLOR | 틸 타이머 | 150 |
| pomo.sunset | POMODORO | COLOR | 선셋 타이머 | 150 |
| pomo.candy | POMODORO | CONCEPT | 캔디 타이머 | 400 |
| planet.crimson | PLANET | COLOR | 진홍 행성 | 250 |
| planet.ice | PLANET | COLOR | 얼음 행성 | 250 |
| planet.ringed | PLANET | CONCEPT | 고리 행성 | 500 |
| celeb.shooting-star | CELEBRATION | CONCEPT | 별똥별 | 500 |
| celeb.ufo | CELEBRATION | CONCEPT | UFO | 600 |
| celeb.golden-rocket | CELEBRATION | CONCEPT | 황금 로켓 | 800 |
| station.mint | STATION | COLOR | 민트 정거장 | 400 |
| station.moonbase | STATION | CONCEPT | 달 기지 | 600 |
| station.mothership | STATION | CONCEPT | 모선 | 600 |
| sticker.heart | (STICKER) | COLOR | 하트 | 80 |
| sticker.important | (STICKER) | COLOR | 중요! | 100 |
| sticker.star | (STICKER) | COLOR | 별 | 100 |
| sticker.fire | (STICKER) | COLOR | 불꽃 | 120 |

스티커의 tier는 의미 없음(UI에서 배지 숨김). 표시 이름은 에셋 선별(Task 4) 결과에 따라 바뀔 수 있으나 **code는 불변**.

---

### Task 1: 백엔드 카탈로그 (`ShopItem` + `ShopCatalog`)

**Files:**
- Create: `backend/src/main/java/com/dumpit/shop/ShopItem.java`
- Create: `backend/src/main/java/com/dumpit/shop/ShopCatalog.java`
- Test: `backend/src/test/java/com/dumpit/shop/ShopCatalogTest.java`

**Interfaces:**
- Produces: `ShopItem(String code, ItemType type, Slot slot, String name, String description, int price, Tier tier)`, `ShopItem.ItemType{THEME,STICKER}`, `ShopItem.Slot{BACKGROUND,CHROME,POMODORO,PLANET,CELEBRATION,STATION}`, `ShopItem.Tier{COLOR,CONCEPT}`
- Produces: `@Service ShopCatalog` — `List<ShopItem> getAll()`, `Optional<ShopItem> findByCode(String code)`

- [ ] **Step 1: 실패하는 테스트 작성**

```java
package com.dumpit.shop;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

class ShopCatalogTest {

    private final ShopCatalog catalog = new ShopCatalog();

    @Test
    void 카탈로그는_22개_아이템을_가진다() {
        assertThat(catalog.getAll()).hasSize(22);
    }

    @Test
    void 코드는_전부_유일하다() {
        assertThat(catalog.getAll().stream().map(ShopItem::code).distinct()).hasSize(22);
    }

    @Test
    void 테마는_슬롯을_갖고_스티커는_슬롯이_없다() {
        assertThat(catalog.getAll()).allSatisfy(item -> {
            if (item.type() == ShopItem.ItemType.THEME) assertThat(item.slot()).isNotNull();
            else assertThat(item.slot()).isNull();
        });
    }

    @Test
    void findByCode는_존재하면_아이템_없으면_빈값() {
        assertThat(catalog.findByCode("bg.ocean")).isPresent();
        assertThat(catalog.findByCode("no.such")).isEmpty();
    }
}
```

- [ ] **Step 2: 컴파일 실패 확인** — `./gradlew compileTestJava` (Global Constraints의 JAVA_HOME 지정). Expected: FAIL (`ShopCatalog` 미존재)

- [ ] **Step 3: 구현**

```java
// ShopItem.java
package com.dumpit.shop;

public record ShopItem(
        String code, ItemType type, Slot slot,
        String name, String description, int price, Tier tier) {

    public enum ItemType { THEME, STICKER }
    public enum Slot { BACKGROUND, CHROME, POMODORO, PLANET, CELEBRATION, STATION }
    public enum Tier { COLOR, CONCEPT }

    public static ShopItem theme(String code, Slot slot, String name, String description, int price, Tier tier) {
        return new ShopItem(code, ItemType.THEME, slot, name, description, price, tier);
    }

    public static ShopItem sticker(String code, String name, String description, int price) {
        return new ShopItem(code, ItemType.STICKER, null, name, description, price, Tier.COLOR);
    }
}
```

```java
// ShopCatalog.java — 카탈로그의 유일한 원본. 다른 곳에서 아이템 목록을 하드코딩하지 말 것.
// (추후 DB 카탈로그로 전환 시 이 클래스만 JPA 구현으로 교체한다)
package com.dumpit.shop;

import org.springframework.stereotype.Service;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

import static com.dumpit.shop.ShopItem.Slot.*;
import static com.dumpit.shop.ShopItem.Tier.*;

@Service
public class ShopCatalog {

    private static final List<ShopItem> ITEMS = List.of(
        ShopItem.theme("bg.ocean",    BACKGROUND, "오션",     "차분한 바다빛 팔레트예요.",        200, COLOR),
        ShopItem.theme("bg.lavender", BACKGROUND, "라벤더",   "은은한 보랏빛 팔레트예요.",        200, COLOR),
        ShopItem.theme("bg.sprout",   BACKGROUND, "새싹 정원", "초록이 가득한 정원 테마예요.",      600, CONCEPT),
        ShopItem.theme("chrome.mint", CHROME,     "민트",     "메뉴가 민트빛으로 물들어요.",       150, COLOR),
        ShopItem.theme("chrome.rose", CHROME,     "로즈",     "메뉴가 장밋빛으로 물들어요.",       150, COLOR),
        ShopItem.theme("chrome.wood", CHROME,     "원목 서재", "따뜻한 원목 책상 느낌이에요.",      400, CONCEPT),
        ShopItem.theme("pomo.teal",   POMODORO,   "틸 타이머",  "집중 링이 틸 색으로 바뀌어요.",     150, COLOR),
        ShopItem.theme("pomo.sunset", POMODORO,   "선셋 타이머", "노을빛 집중 링이에요.",           150, COLOR),
        ShopItem.theme("pomo.candy",  POMODORO,   "캔디 타이머", "달콤한 캔디 배색 타이머예요.",     400, CONCEPT),
        ShopItem.theme("planet.crimson", PLANET,  "진홍 행성",  "붉게 타오르는 행성이에요.",        250, COLOR),
        ShopItem.theme("planet.ice",     PLANET,  "얼음 행성",  "차갑게 빛나는 행성이에요.",        250, COLOR),
        ShopItem.theme("planet.ringed",  PLANET,  "고리 행성",  "멋진 고리를 두른 행성이에요.",      500, CONCEPT),
        ShopItem.theme("celeb.shooting-star", CELEBRATION, "별똥별",   "완료 축하로 별똥별이 지나가요.",  500, CONCEPT),
        ShopItem.theme("celeb.ufo",           CELEBRATION, "UFO",     "완료 축하로 UFO가 날아올라요.",   600, CONCEPT),
        ShopItem.theme("celeb.golden-rocket", CELEBRATION, "황금 로켓", "반짝이는 황금 로켓 발사!",       800, CONCEPT),
        ShopItem.theme("station.mint",       STATION, "민트 정거장", "민트빛 우주정거장이에요.",       400, COLOR),
        ShopItem.theme("station.moonbase",   STATION, "달 기지",    "달 표면의 아늑한 기지예요.",      600, CONCEPT),
        ShopItem.theme("station.mothership", STATION, "모선",      "웅장한 모선이에요.",             600, CONCEPT),
        ShopItem.sticker("sticker.heart",     "하트",  "마음이 가는 항목에 붙여요.",  80),
        ShopItem.sticker("sticker.important", "중요!", "놓치면 안 되는 항목에 붙여요.", 100),
        ShopItem.sticker("sticker.star",      "별",    "빛나는 항목에 붙여요.",       100),
        ShopItem.sticker("sticker.fire",      "불꽃",  "지금 불타는 항목에 붙여요.",   120)
    );

    private static final Map<String, ShopItem> BY_CODE =
            ITEMS.stream().collect(Collectors.toUnmodifiableMap(ShopItem::code, Function.identity()));

    public List<ShopItem> getAll() { return ITEMS; }

    public Optional<ShopItem> findByCode(String code) {
        return Optional.ofNullable(code == null ? null : BY_CODE.get(code));
    }
}
```

- [ ] **Step 4: 테스트 통과 확인** — `./gradlew test --tests "com.dumpit.shop.ShopCatalogTest"`. Expected: PASS 4/4
- [ ] **Step 5: Commit** — `Feat: 상점 카탈로그 정의(ShopItem/ShopCatalog) 추가`

---

### Task 2: DB·엔티티 개편 + 구매/장착 서비스 + ShopController 재작성

**Files:**
- Create: `backend/src/main/resources/db/migration/V3__shop_items_and_stickers.sql`
- Create: `backend/src/main/java/com/dumpit/entity/UserEquipment.java`
- Create: `backend/src/main/java/com/dumpit/repository/UserEquipmentRepository.java`
- Create: `backend/src/main/java/com/dumpit/service/ShopService.java` (인터페이스 없이 단일 클래스 — 기존 서비스 패턴 확인 후 `service.impl` 패턴이면 그에 맞춤)
- Modify: `backend/src/main/java/com/dumpit/entity/UserPurchase.java` (itemId→itemCode)
- Modify: `backend/src/main/java/com/dumpit/repository/PurchaseRepository.java`
- Modify: `backend/src/main/java/com/dumpit/controller/ShopController.java` (전면 재작성)
- Test: `backend/src/test/java/com/dumpit/service/ShopServiceTest.java`

**Interfaces:**
- Consumes: Task 1의 `ShopCatalog.findByCode/getAll`, `ShopItem`
- Produces:
  - `ShopService.getCatalog(String email)` → `CatalogResponse(int coinBalance, List<CatalogItem> items)`; `CatalogItem(String code, String type, String slot, String name, String description, int price, String tier, boolean owned, boolean equipped)`
  - `ShopService.purchase(String email, String code)` → `PurchaseResult(int remainingCoins, boolean equipped)`
  - `ShopService.equip(String email, String code)` / `ShopService.unequip(String email, ShopItem.Slot slot)`
  - `ShopService.getEquipments(User user)` → `Map<String,String>` (슬롯명→코드)
  - `ShopService.assertOwnsSticker(User user, String code)` — Task 3의 스티커 API가 사용
  - `PurchaseRepository.existsByUserAndItemCode(User, String)`
  - `UserEquipmentRepository.findByUser(User)`, `findByUserAndSlot(User, String)`, `deleteByUserAndSlot(User, String)`

- [ ] **Step 1: Flyway V3 작성** — V1이 no-op baseline이라 **신규 DB에서는 이 마이그레이션이 Hibernate 테이블 생성보다 먼저 실행됨** → 전부 존재 가드 필수. 신규 DB에서는 전 구문이 스킵되고 Hibernate(ddl-auto:update)가 새 엔티티 정의대로 생성한다.

```sql
-- V3: 상점 개편 — user_purchases.item_id → item_code, user_equipments 신설, 스티커 칼럼
-- 주의: 신규(빈) DB에서는 테이블이 아직 없으므로 모든 구문을 존재 가드로 감싼다.

DO $$
BEGIN
  -- 1) user_purchases: int item_id → varchar item_code (구 카탈로그 폐기, 실구매 0건 전제)
  IF to_regclass('public.user_purchases') IS NOT NULL
     AND EXISTS (SELECT 1 FROM information_schema.columns
                 WHERE table_schema='public' AND table_name='user_purchases' AND column_name='item_id') THEN
    DELETE FROM user_purchases;
    ALTER TABLE user_purchases DROP COLUMN item_id;
    ALTER TABLE user_purchases ADD COLUMN item_code varchar(64) NOT NULL DEFAULT '';
    ALTER TABLE user_purchases ALTER COLUMN item_code DROP DEFAULT;
  END IF;

  IF to_regclass('public.user_purchases') IS NOT NULL
     AND NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'uk_user_purchases_user_item') THEN
    ALTER TABLE user_purchases ADD CONSTRAINT uk_user_purchases_user_item UNIQUE (user_id, item_code);
  END IF;

  -- 2) user_equipments (users가 있을 때만 — 신규 DB는 Hibernate가 생성)
  IF to_regclass('public.users') IS NOT NULL AND to_regclass('public.user_equipments') IS NULL THEN
    CREATE TABLE user_equipments (
      equipment_id uuid PRIMARY KEY,
      user_id      uuid NOT NULL REFERENCES users(user_id),
      slot         varchar(20) NOT NULL,
      item_code    varchar(64) NOT NULL,
      updated_at   timestamp,
      CONSTRAINT uk_user_equipments_user_slot UNIQUE (user_id, slot)
    );
  END IF;

  -- 3) 스티커 칼럼 (nullable)
  IF to_regclass('public.tasks') IS NOT NULL THEN
    ALTER TABLE tasks ADD COLUMN IF NOT EXISTS sticker_code varchar(64);
  END IF;
  IF to_regclass('public.ideas') IS NOT NULL THEN
    ALTER TABLE ideas ADD COLUMN IF NOT EXISTS sticker_code varchar(64);
  END IF;
END $$;
```

- [ ] **Step 2: 엔티티 수정**

`UserPurchase.java`: `Integer itemId` 필드·`of(User,int,int)`를 다음으로 교체 (유니크 제약 이름 포함):

```java
@Table(name = "user_purchases",
       uniqueConstraints = @UniqueConstraint(name = "uk_user_purchases_user_item",
                                             columnNames = {"user_id", "item_code"}))
// ...
@Column(name = "item_code", nullable = false, length = 64)
private String itemCode;

public static UserPurchase of(User user, String itemCode, int price) {
    UserPurchase p = new UserPurchase();
    p.user = user;
    p.itemCode = itemCode;
    p.price = price;
    return p;
}
```

`UserEquipment.java` 신설:

```java
package com.dumpit.entity;

import jakarta.persistence.*;
import lombok.Getter;
import lombok.NoArgsConstructor;
import org.hibernate.annotations.UpdateTimestamp;

import java.time.LocalDateTime;
import java.util.UUID;

@Entity
@Table(name = "user_equipments",
       uniqueConstraints = @UniqueConstraint(name = "uk_user_equipments_user_slot",
                                             columnNames = {"user_id", "slot"}))
@Getter
@NoArgsConstructor
public class UserEquipment {

    @Id
    @GeneratedValue(strategy = GenerationType.UUID)
    @Column(name = "equipment_id")
    private UUID equipmentId;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id", nullable = false)
    private User user;

    @Column(nullable = false, length = 20)
    private String slot;

    @Column(name = "item_code", nullable = false, length = 64)
    private String itemCode;

    @UpdateTimestamp
    private LocalDateTime updatedAt;

    public static UserEquipment of(User user, String slot, String itemCode) {
        UserEquipment e = new UserEquipment();
        e.user = user;
        e.slot = slot;
        e.itemCode = itemCode;
        return e;
    }

    public void changeItem(String itemCode) { this.itemCode = itemCode; }
}
```

`Task.java`·`Idea.java`: `@Column(name = "sticker_code", length = 64) private String stickerCode;` 추가 + 엔티티의 기존 수정 메서드 스타일대로 setter/변경 메서드 추가 (Task가 `@Setter`면 자동, 아니면 `updateSticker(String)` 메서드).

- [ ] **Step 3: 리포지토리**

```java
// PurchaseRepository — existsByUserAndItemId 를 대체
List<UserPurchase> findByUser(User user);
boolean existsByUserAndItemCode(User user, String itemCode);
```

```java
// UserEquipmentRepository.java
package com.dumpit.repository;

import com.dumpit.entity.User;
import com.dumpit.entity.UserEquipment;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;
import java.util.Optional;
import java.util.UUID;

public interface UserEquipmentRepository extends JpaRepository<UserEquipment, UUID> {
    List<UserEquipment> findByUser(User user);
    Optional<UserEquipment> findByUserAndSlot(User user, String slot);
    void deleteByUserAndSlot(User user, String slot);
}
```

- [ ] **Step 4: 실패하는 ShopService 테스트 작성** — 기존 `TaskPlanningServiceImplTest`의 Mockito 스타일을 따른다.

```java
package com.dumpit.service;
// (실제 패키지는 기존 서비스 테스트 위치 관례를 따를 것)

import ...

@ExtendWith(MockitoExtension.class)
class ShopServiceTest {

    @Mock UserRepository userRepository;
    @Mock PurchaseRepository purchaseRepository;
    @Mock UserEquipmentRepository equipmentRepository;
    ShopCatalog catalog = new ShopCatalog();
    ShopService shopService; // @BeforeEach에서 new ShopService(catalog, userRepository, purchaseRepository, equipmentRepository)

    // 헬퍼: 코인 잔액 있는 유저 생성 (User.of(...) 후 addCoins)

    @Test void 구매_성공시_코인차감_기록_자동장착() { /* bg.ocean 200코인, 잔액 500 → remainingCoins 300, purchaseRepository.save + equipmentRepository.save 검증, equipped=true */ }
    @Test void 스티커_구매는_장착하지_않는다() { /* sticker.heart → equipmentRepository.save 호출 없음, equipped=false */ }
    @Test void 존재하지_않는_코드는_400() { /* assertThrows BadRequestException */ }
    @Test void 이미_보유한_아이템은_400() { /* existsByUserAndItemCode true */ }
    @Test void 잔액_부족은_400_그리고_기록없음() { /* 잔액 100 < 200, save 호출 없음 검증 */ }
    @Test void 장착은_보유_아이템만() { /* equip 미보유 → BadRequestException */ }
    @Test void 장착은_같은_슬롯을_교체한다() { /* 기존 UserEquipment 존재 시 changeItem 호출/저장 */ }
    @Test void 스티커는_장착_불가() { /* equip(sticker.heart) → BadRequestException */ }
    @Test void 해제는_행을_삭제한다() { /* unequip → deleteByUserAndSlot */ }
    @Test void assertOwnsSticker_미보유면_400_보유면_통과() {}
    @Test void assertOwnsSticker_테마코드는_400() { /* bg.ocean은 스티커 아님 */ }
}
```

각 테스트 본문은 위 주석 시나리오를 그대로 구현한다(11개 모두 — 생략 금지).

- [ ] **Step 5: 컴파일 실패 확인** — Expected: FAIL (`ShopService` 미존재)

- [ ] **Step 6: ShopService 구현**

```java
package com.dumpit.service;

import com.dumpit.entity.*;
import com.dumpit.exception.BadRequestException;
import com.dumpit.exception.NotFoundException;
import com.dumpit.repository.*;
import com.dumpit.shop.ShopCatalog;
import com.dumpit.shop.ShopItem;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.*;
import java.util.stream.Collectors;

@Service
@RequiredArgsConstructor
public class ShopService {

    private final ShopCatalog catalog;
    private final UserRepository userRepository;
    private final PurchaseRepository purchaseRepository;
    private final UserEquipmentRepository equipmentRepository;

    public record CatalogItem(String code, String type, String slot, String name, String description,
                              int price, String tier, boolean owned, boolean equipped) {}
    public record CatalogResponse(int coinBalance, List<CatalogItem> items) {}
    public record PurchaseResult(int remainingCoins, boolean equipped) {}

    @Transactional(readOnly = true)
    public CatalogResponse getCatalog(String email) {
        User user = findUser(email);
        Set<String> owned = purchaseRepository.findByUser(user).stream()
                .map(UserPurchase::getItemCode).collect(Collectors.toSet());
        Map<String, String> equipped = getEquipments(user); // slot명 → code

        List<CatalogItem> items = catalog.getAll().stream()
                .map(i -> new CatalogItem(
                        i.code(), i.type().name(),
                        i.slot() != null ? i.slot().name() : null,
                        i.name(), i.description(), i.price(), i.tier().name(),
                        owned.contains(i.code()),
                        i.slot() != null && i.code().equals(equipped.get(i.slot().name()))))
                .toList();
        return new CatalogResponse(user.getCoinBalance(), items);
    }

    @Transactional
    public PurchaseResult purchase(String email, String code) {
        User user = findUser(email);
        ShopItem item = catalog.findByCode(code)
                .orElseThrow(() -> new BadRequestException("존재하지 않는 아이템입니다."));
        if (purchaseRepository.existsByUserAndItemCode(user, code)) {
            throw new BadRequestException("이미 보유한 아이템입니다.");
        }
        if (!user.spendCoins(item.price())) {
            throw new BadRequestException("코인이 부족합니다.");
        }
        userRepository.save(user);
        purchaseRepository.save(UserPurchase.of(user, code, item.price()));

        boolean equipped = false;
        if (item.type() == ShopItem.ItemType.THEME) {
            upsertEquipment(user, item);
            equipped = true;
        }
        return new PurchaseResult(user.getCoinBalance(), equipped);
    }

    @Transactional
    public void equip(String email, String code) {
        User user = findUser(email);
        ShopItem item = catalog.findByCode(code)
                .orElseThrow(() -> new BadRequestException("존재하지 않는 아이템입니다."));
        if (item.type() != ShopItem.ItemType.THEME) {
            throw new BadRequestException("장착할 수 없는 아이템입니다.");
        }
        if (!purchaseRepository.existsByUserAndItemCode(user, code)) {
            throw new BadRequestException("보유하지 않은 아이템입니다.");
        }
        upsertEquipment(user, item);
    }

    @Transactional
    public void unequip(String email, ShopItem.Slot slot) {
        User user = findUser(email);
        equipmentRepository.deleteByUserAndSlot(user, slot.name());
    }

    @Transactional(readOnly = true)
    public Map<String, String> getEquipments(User user) {
        return equipmentRepository.findByUser(user).stream()
                .collect(Collectors.toMap(UserEquipment::getSlot, UserEquipment::getItemCode));
    }

    @Transactional(readOnly = true)
    public void assertOwnsSticker(User user, String code) {
        ShopItem item = catalog.findByCode(code)
                .orElseThrow(() -> new BadRequestException("존재하지 않는 스티커입니다."));
        if (item.type() != ShopItem.ItemType.STICKER) {
            throw new BadRequestException("스티커가 아닌 아이템입니다.");
        }
        if (!purchaseRepository.existsByUserAndItemCode(user, code)) {
            throw new BadRequestException("보유하지 않은 스티커입니다.");
        }
    }

    private void upsertEquipment(User user, ShopItem item) {
        String slot = item.slot().name();
        equipmentRepository.findByUserAndSlot(user, slot)
                .ifPresentOrElse(
                        e -> { e.changeItem(item.code()); equipmentRepository.save(e); },
                        () -> equipmentRepository.save(UserEquipment.of(user, slot, item.code())));
    }

    private User findUser(String email) {
        return userRepository.findByEmail(email)
                .orElseThrow(() -> new NotFoundException("사용자를 찾을 수 없습니다."));
    }
}
```

- [ ] **Step 7: ShopController 전면 재작성** — 기존 CATALOG 상수·record 3종 삭제.

```java
package com.dumpit.controller;

import com.dumpit.service.ShopService;
import com.dumpit.shop.ShopItem;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.security.oauth2.core.user.OAuth2User;
import org.springframework.web.bind.annotation.*;

import java.util.Map;

@RestController
@RequestMapping("/shop")
@RequiredArgsConstructor
public class ShopController {

    private final ShopService shopService;

    @GetMapping("/catalog")
    public ResponseEntity<ShopService.CatalogResponse> getCatalog(@AuthenticationPrincipal OAuth2User principal) {
        return ResponseEntity.ok(shopService.getCatalog(principal.getAttribute("email")));
    }

    @PostMapping("/purchase")
    public ResponseEntity<?> purchase(@AuthenticationPrincipal OAuth2User principal,
                                      @RequestBody CodeRequest req) {
        ShopService.PurchaseResult result = shopService.purchase(principal.getAttribute("email"), req.code());
        return ResponseEntity.ok(Map.of(
                "message", "구매 완료!",
                "remainingCoins", result.remainingCoins(),
                "equipped", result.equipped()));
    }

    @PutMapping("/equip")
    public ResponseEntity<?> equip(@AuthenticationPrincipal OAuth2User principal,
                                   @RequestBody CodeRequest req) {
        shopService.equip(principal.getAttribute("email"), req.code());
        return ResponseEntity.ok(Map.of("message", "장착했어요."));
    }

    @DeleteMapping("/equip/{slot}")
    public ResponseEntity<?> unequip(@AuthenticationPrincipal OAuth2User principal,
                                     @PathVariable("slot") ShopItem.Slot slot) {
        shopService.unequip(principal.getAttribute("email"), slot);
        return ResponseEntity.ok(Map.of("message", "기본으로 되돌렸어요."));
    }

    public record CodeRequest(String code) {}
}
```

주의: `GET /shop/items`는 삭제된다. 이를 쓰던 `SettingsModal.jsx`는 Task 8에서 `/shop/catalog`로 갱신 — 백엔드 머지 시점과 프론트 태스크 사이에 dev에서 일시적 불일치가 생기지만 로컬 개발에만 영향, 허용.

- [ ] **Step 8: 전체 테스트** — `./gradlew test`. Expected: 신규 11개 포함 전부 PASS. `BadRequestException`/`NotFoundException` 생성자 시그니처는 기존 코드 확인 후 맞출 것.
- [ ] **Step 9: 로컬 마이그레이션 확인** — 로컬 PostgreSQL(dumpit/dumpit@localhost:5432/dumpit)에서 백엔드 기동 → Flyway V3 적용 로그 확인, `\d user_purchases`에 item_code, `\d user_equipments` 존재 확인.
- [ ] **Step 10: Commit** — `Feat: 상점 구매·장착 백엔드(V3 마이그레이션, ShopService) 구현`

---

### Task 3: `/auth/me` equipments + 스티커 부착 API + DTO 반영

**Files:**
- Modify: `backend/src/main/java/com/dumpit/controller/AuthController.java`
- Modify: `backend/src/main/java/com/dumpit/controller/TaskController.java`, `IdeaController.java`
- Modify: `backend/src/main/java/com/dumpit/service/TaskService.java`(인터페이스)·`impl/TaskServiceImpl.java`, `IdeaService`·`impl/IdeaServiceImpl.java`
- Modify: `backend/src/main/java/com/dumpit/dto/TaskResponse.java`, `IdeaResponse.java`
- Test: `backend/src/test/java/com/dumpit/service/ShopServiceTest.java`(스티커 검증은 Task 2에서 완료) + 기존 서비스 테스트 컴파일 유지

**Interfaces:**
- Consumes: `ShopService.assertOwnsSticker(User, String)`, `ShopService.getEquipments(User)`
- Produces:
  - `GET /auth/me` → `UserMeResponse(email, name, picture, coins, isAdmin, Map<String,String> equipments)`
  - `PUT /tasks/{taskId}/sticker` body `{"code": "sticker.heart" | null}` → `TaskResponse` (stickerCode 포함)
  - `PUT /ideas/{ideaId}/sticker` body 동일 → `IdeaResponse`
  - `TaskResponse.stickerCode`, `IdeaResponse.stickerCode` 필드

- [ ] **Step 1: AuthController** — `UserMeResponse`에 `Map<String,String> equipments` 추가, `me()`에서 `shopService.getEquipments(user)` 주입. (`ShopService` 필드 추가)
- [ ] **Step 2: 스티커 세터 서비스 메서드** — `TaskService`에 `Task updateSticker(String email, UUID taskId, String code)` 추가. 구현(TaskServiceImpl): 태스크 로드+소유자 검증(기존 조회 패턴 재사용), `code != null`이면 `shopService.assertOwnsSticker(task.getUser(), code)` 후 `task`의 stickerCode 갱신, null이면 제거. `IdeaService.updateSticker`도 동일 패턴.
- [ ] **Step 3: 컨트롤러 엔드포인트**

```java
// TaskController에 추가
@PutMapping("/{taskId}/sticker")
public ResponseEntity<TaskResponse> updateSticker(
        @AuthenticationPrincipal OAuth2User principal,
        @PathVariable("taskId") UUID taskId,
        @RequestBody StickerRequest req) {
    Task task = taskService.updateSticker(principal.getAttribute("email"), taskId, req.code());
    return ResponseEntity.ok(TaskResponse.from(task));
}
public record StickerRequest(String code) {}
```

IdeaController에도 동일 형태(`PUT /{ideaId}/sticker` → `IdeaResponse`).

- [ ] **Step 4: DTO** — `TaskResponse`·`IdeaResponse` record에 `String stickerCode` 필드 추가 + `from()`에서 `t.getStickerCode()` 매핑. (플래닝 응답은 TaskResponse를 재사용하므로 자동 반영 — `TaskPlanningResponse`가 자체 DTO를 쓰면 거기도 추가)
- [ ] **Step 5: 전체 테스트** — `./gradlew test` PASS. TaskResponse 필드 추가로 깨지는 기존 테스트의 생성자 호출부 수정.
- [ ] **Step 6: Commit** — `Feat: 장착 상태 프로필 노출 및 태스크·아이디어 스티커 API 추가`

---

### Task 4: 에셋 수집·가공 (⚠️ 컨트롤러 직접 수행 — 웹 접근·선별 판단 필요)

**Files:**
- Create: `frontend/src/assets/shop/` 아래 **정확히 이 16개 파일** (다운스트림 태스크가 이 이름을 import):
  - `planet_default.png`, `planet_crimson.png`, `planet_ice.png`, `planet_ringed.png`
  - `celeb_rocket_default.png`, `celeb_shooting_star.png`, `celeb_ufo.png`, `celeb_golden_rocket.png`
  - `station_default.png`, `station_mint.png`, `station_moonbase.png`, `station_mothership.png`
  - `sticker_heart.png`, `sticker_important.png`, `sticker_star.png`, `sticker_fire.png`
- Create: `docs/ATTRIBUTIONS.md`

**Interfaces:**
- Produces: 위 16개 PNG (원본 픽셀 해상도 유지 — 행성 16~32px, 정거장 32~64px, 완료 스프라이트 16~48px, 스티커 16px 내외. 업스케일은 CSS `image-rendering: pixelated`가 담당)

- [ ] **Step 1: 후보 팩 조사·라이선스 확인** — CC0만. 우선순위: ① OpenGameArt에서 `pixel planet CC0`, `pixel spaceship CC0`, `pixel space station CC0`, `pixel icons 16x16 CC0` 검색(파일 직링크 안정적) ② kenney.nl 픽셀 팩 ③ itch.io CC0 태그. 각 팩 페이지에서 **라이선스 표기를 직접 확인**하고 URL 기록.
- [ ] **Step 2: 다운로드·크롭·리네임** — 스프라이트시트면 개별 스프라이트로 잘라내기(Python Pillow 스크립트, scratchpad에서 작업). 색 변형(진홍/얼음/민트)은 원본 스프라이트의 hue 조정으로 생성해도 됨(Pillow HSV 변환) — CC0라 개작 자유.
- [ ] **Step 3: 배치 검증** — 16개 파일 전부 존재, 각각 20KB 이하(픽셀 원본이라 보통 1~2KB), `git add` 전 육안 확인(뷰어로 열어 스타일 통일성 점검).
- [ ] **Step 4: ATTRIBUTIONS.md 작성** — 팩 이름 / 작가 / 출처 URL / 라이선스(CC0) / 사용 파일 목록 표.
- [ ] **Step 5: Commit** — `Chore: 상점 스프라이트 에셋(CC0) 추가 및 출처 기록`

---

### Task 5: CSS 변수 분리 (`--chrome-*`, `--pomo-*`) — 외관 불변

**Files:**
- Modify: `frontend/src/index.css` (`:root`에 변수 추가)
- Modify: `frontend/tailwind.config.js` (chrome 색 토큰)
- Modify: `frontend/src/components/layout/Header.jsx`, `layout/Sidebar.jsx`, `PomodoroTimer.jsx`

**Interfaces:**
- Produces: CSS 변수 `--chrome-bg`, `--chrome-line`, `--pomo-focus`, `--pomo-break`, `--pomo-ring`; Tailwind 클래스 `bg-chrome`, `border-chrome-line`

- [ ] **Step 1: 변수 선언** — `index.css`의 `:root` 블록 끝에 추가 (다크 블록에는 **추가하지 않는다** — var() 참조라 자동 추종):

```css
/* 상점 스킨용 파생 변수 — 기본값은 기존 색을 그대로 참조 (스킨 미장착 시 외관 불변) */
--chrome-bg:var(--card); --chrome-line:var(--line);
--pomo-focus:var(--accent); --pomo-break:var(--accent2); --pomo-ring:var(--line);
```

- [ ] **Step 2: Tailwind 토큰** — `tailwind.config.js` colors에 `chrome: 'var(--chrome-bg)'`, `'chrome-line': 'var(--chrome-line)'` 추가.
- [ ] **Step 3: 적용 교체**
  - `Header.jsx`: `<header className="sticky top-0 z-50 bg-card border-b border-line">` → `bg-chrome border-b border-chrome-line`
  - `Sidebar.jsx`: 데스크톱 `<aside>`와 모바일 드로어 `<aside>`의 `bg-card border-r border-line` → `bg-chrome border-r border-chrome-line` (2곳)
  - `PomodoroTimer.jsx`: ① 링 base circle `stroke="var(--line)"` → `var(--pomo-ring)` ② 진행 circle `stroke={isFocus ? 'var(--accent)' : 'var(--accent2)'}` → `var(--pomo-focus)`/`var(--pomo-break)` ③ FOCUS/BREAK 모드 칩의 `bg-primary`/`bg-secondary` 클래스 제거하고 `style={{ background: isFocus ? 'var(--pomo-focus)' : 'var(--pomo-break)' }}`(텍스트는 `text-on-accent` 유지) ④ 시작 버튼의 `bg-primary` → `style={{ background: 'var(--pomo-focus)' }}` (running 상태 `bg-accent`는 그대로)
- [ ] **Step 4: 검증** — `npm run lint` 0 errors / `npm run build` 성공 / dev 서버에서 라이트·다크 모두 **변경 전과 픽셀 동일** 육안 확인.
- [ ] **Step 5: Commit** — `Refactor: 사이드바·헤더·뽀모도로 색상을 스킨용 전용 변수로 분리`

---

### Task 6: 스킨 적용 시스템 (applySkins + CSS 팔레트 9종 + AuthContext 연동)

**Files:**
- Create: `frontend/src/shop/applySkins.js`
- Modify: `frontend/src/index.css` (스킨 블록 추가), `frontend/src/main.jsx`, `frontend/src/context/AuthContext.jsx`

**Interfaces:**
- Consumes: `/auth/me`의 `equipments` 맵 (Task 3)
- Produces: `applySkins(equipments)` — CSS 슬롯 3종을 `<html>` dataset에 반영+캐시. `user.equipments`는 AuthContext user 객체에 그대로 존재(스프라이트 슬롯은 Task 7이 소비)

- [ ] **Step 1: applySkins.js**

```js
// 서버가 원본, localStorage는 첫 페인트 번쩍임 방지용 캐시
const CACHE_KEY = 'dumpit_equipments'
// CSS로 적용되는 슬롯만 dataset에 반영 (스프라이트 슬롯은 컴포넌트가 user.equipments에서 직접 읽음)
const CSS_SLOTS = { BACKGROUND: 'skinBg', CHROME: 'skinChrome', POMODORO: 'skinPomodoro' }

export function applySkins(equipments) {
  const eq = equipments || {}
  for (const [slot, dataKey] of Object.entries(CSS_SLOTS)) {
    const code = eq[slot]
    if (code) document.documentElement.dataset[dataKey] = code.split('.').pop()
    else delete document.documentElement.dataset[dataKey]
  }
  try { localStorage.setItem(CACHE_KEY, JSON.stringify(eq)) } catch {}
}

export function applyCachedSkins() {
  try {
    const cached = localStorage.getItem(CACHE_KEY)
    if (cached) applySkins(JSON.parse(cached))
  } catch {}
}

export function clearSkins() {
  try { localStorage.removeItem(CACHE_KEY) } catch {}
  applySkins(null)
}
```

- [ ] **Step 2: main.jsx** — 렌더 전에 `applyCachedSkins()` 호출(동기, import 후 첫 줄 수준).
- [ ] **Step 3: AuthContext 연동** — `fetchUser`/`refreshCoins`의 성공 콜백에서 `setUser` 직후 `applySkins(nextUser?.equipments)` (유저 null이면 `clearSkins()`), `logout()`에서 `clearSkins()`. 두 콜백의 중복을 없애려면 공통 `handleMeResponse(res)` 헬퍼로 합칠 것.
- [ ] **Step 4: 스킨 CSS 블록** — `index.css` 끝에 추가. **순서 중요: bg 스킨 블록 먼저, chrome 스킨 블록을 그 뒤에**(동일 특이성에서 chrome이 이겨야 함). 아래 값은 시작점 — Task 10 육안 QA에서 미세 조정 허용.

```css
/* ==== 상점 스킨: 배경&주요색상 (라이트+다크 각 1벌) ==== */
[data-skin-bg="ocean"] {
  --bg:#E4EFEC; --card:#FCFEFD; --chip:#D3E6E1; --line:#B7D4CD; --edge:#28423C;
  --accent:#2E7D8A; --accent2:#D97757; --on-accent:#F4FBF9;
  --shadow-hero:#A9CEC5; --shadow-sm:#C6DDD7; --overlay:rgba(40,66,60,.35);
}
[data-skin-bg="ocean"][data-theme="dark"] {
  --bg:#152430; --card:#1E3240; --chip:#28414F; --line:#375366; --edge:#0B141C;
  --accent:#5FB8C9; --accent2:#F09355; --on-accent:#0F1D26;
  --shadow-hero:#0B141C; --shadow-sm:#0B141C; --overlay:rgba(5,12,18,.55);
}
[data-skin-bg="lavender"] {
  --bg:#EEEAF4; --card:#FDFCFF; --chip:#E2DAEC; --line:#CBBEDC; --edge:#372C48;
  --accent:#8A63C4; --accent2:#3E8E85; --on-accent:#FBF9FF;
  --shadow-hero:#CDBEE2; --shadow-sm:#D8CCE8; --overlay:rgba(55,44,72,.35);
}
[data-skin-bg="lavender"][data-theme="dark"] {
  --bg:#251D3A; --card:#32294E; --chip:#40355F; --line:#4F4373; --edge:#151022;
  --accent:#B79CE8; --accent2:#5FC4B4; --on-accent:#1D1630;
  --shadow-hero:#151022; --shadow-sm:#151022; --overlay:rgba(12,8,22,.55);
}
[data-skin-bg="sprout"] {
  --bg:#EAF2E3; --card:#FBFEF7; --chip:#DCEBCE; --line:#C2DBAA; --edge:#2F4224;
  --accent:#5C8A3C; --accent2:#C4708F; --on-accent:#F7FCF0;
  --shadow-hero:#BFD8A6; --shadow-sm:#CFE2BA; --overlay:rgba(47,66,36,.35);
}
[data-skin-bg="sprout"][data-theme="dark"] {
  --bg:#1B2617; --card:#26351F; --chip:#31452A; --line:#40573A; --edge:#0E150B;
  --accent:#8FBF6F; --accent2:#D98BA6; --on-accent:#131C0E;
  --shadow-hero:#0E150B; --shadow-sm:#0E150B; --overlay:rgba(6,10,4,.55);
}

/* ==== 상점 스킨: 사이드바&상단바 — bg 스킨보다 뒤에 위치할 것 ==== */
[data-skin-chrome="mint"] { --chrome-bg:#E9F4F0; --chrome-line:#BFDCD3; }
[data-skin-chrome="mint"][data-theme="dark"] { --chrome-bg:#1D3029; --chrome-line:#35544A; }
[data-skin-chrome="rose"] { --chrome-bg:#F6EAEA; --chrome-line:#E3C4C4; }
[data-skin-chrome="rose"][data-theme="dark"] { --chrome-bg:#2E2028; --chrome-line:#503846; }
[data-skin-chrome="wood"] { --chrome-bg:#F1E5D2; --chrome-line:#D6BE97; }
[data-skin-chrome="wood"][data-theme="dark"] { --chrome-bg:#2A2118; --chrome-line:#4A3A28; }

/* ==== 상점 스킨: 뽀모도로 ==== */
[data-skin-pomodoro="teal"] { --pomo-focus:#3E8E85; --pomo-break:#D98E2B; }
[data-skin-pomodoro="teal"][data-theme="dark"] { --pomo-focus:#5FC4B4; --pomo-break:#E9B44C; }
[data-skin-pomodoro="sunset"] { --pomo-focus:#E0703C; --pomo-break:#8A63C4; }
[data-skin-pomodoro="sunset"][data-theme="dark"] { --pomo-focus:#F09355; --pomo-break:#B79CE8; }
[data-skin-pomodoro="candy"] { --pomo-focus:#E05C8A; --pomo-break:#5CA8E0; --pomo-ring:#F0C4D8; }
[data-skin-pomodoro="candy"][data-theme="dark"] { --pomo-focus:#F08CAE; --pomo-break:#7FB8E8; --pomo-ring:#5A3A48; }
```

주의: 스킨 코드 값은 `code.split('.').pop()` 결과(`ocean`, `shooting-star` 등)와 정확히 일치해야 함.

- [ ] **Step 5: 검증** — lint/build 통과. dev 서버 콘솔에서 `document.documentElement.dataset.skinBg='ocean'` 수동 주입 → 라이트/다크 각각 팔레트 전환 확인, 속성 제거 시 원복 확인.
- [ ] **Step 6: Commit** — `Feat: 스킨 적용 시스템(applySkins)과 색상 테마 9종 CSS 추가`

---

### Task 7: 스프라이트 레지스트리 + 행성/완료/정거장 컴포넌트 교체

**Files:**
- Create: `frontend/src/shop/registry.js`
- Modify: `frontend/src/components/OrbitProgress.jsx`, `RocketLaunch.jsx`, `PixelStation.jsx`
- Modify: `frontend/src/index.css` (`.pixel-planet`·`.pixel-station`·`.rocket-body`·`.rocket-trail` box-shadow 아트 삭제, 스프라이트 클래스 추가)

**Interfaces:**
- Consumes: Task 4의 에셋 16종, `useAuth().user?.equipments` (Task 3/6)
- Produces: `registry.js` exports —

```js
// 각 맵: { [코드 또는 'default']: { name: string, img: string } }
export const PLANET_SPRITES = { default, 'planet.crimson', 'planet.ice', 'planet.ringed' }
export const CELEBRATION_SPRITES = { default, 'celeb.shooting-star', 'celeb.ufo', 'celeb.golden-rocket' }
export const STATION_SPRITES = { default, 'station.mint', 'station.moonbase', 'station.mothership' }
export const STICKER_SPRITES = { 'sticker.heart', 'sticker.important', 'sticker.star', 'sticker.fire' }
export function spriteFor(map, code) { return map[code] ?? map.default }
```

- [ ] **Step 1: registry.js 작성** — 16개 에셋 import, 위 구조 그대로. `name`은 카탈로그 확정본의 이름과 일치.
- [ ] **Step 2: OrbitProgress** — `.pixel-planet` div를 `<img src={spriteFor(PLANET_SPRITES, user?.equipments?.PLANET).img} alt="" className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[22px] h-[22px]" style={{ imageRendering: 'pixelated' }} />`로 교체. `useAuth` 훅 사용. 궤도 링·위성(`orbit-sat`)은 유지.
- [ ] **Step 3: RocketLaunch** — `.rocket-body`/`.rocket-trail` div를 스프라이트 `<img>` 1장으로 교체(클래스 `celebration-sprite`), 코드가 `user?.equipments?.CELEBRATION`. index.css에:

```css
.celebration-sprite {
  position: absolute; left: 50%; bottom: -12vh; width: 44px; height: auto;
  image-rendering: pixelated; transform: translateX(-50%);
  animation: rocket-up 1.4s ease-in forwards;
}
```

`rocket-up` 키프레임의 `transform`이 `translateX(-50%)`를 덮어쓰지 않도록 키프레임을 `translate(-50%, 0)`→`translate(-50%, -130vh)`로 수정. `prefers-reduced-motion` 블록의 `.rocket-body, .rocket-trail` 셀렉터를 `.celebration-sprite`로 교체. 모든 완료 테마는 동일 비행 연출(연출 개별화는 후속 범위 아님).
- [ ] **Step 4: PixelStation** — `.pixel-station` div를 `<img … className="h-16 w-auto" style={{ imageRendering: 'pixelated' }} />`로, 코드 `user?.equipments?.STATION`.
- [ ] **Step 5: index.css 정리** — `.pixel-planet`, `.pixel-station`, `.rocket-body`, `.rocket-trail` 블록과 이제 안 쓰는 `--p1/--p2/--p3` 참조 여부 확인 후 삭제(다른 사용처 grep 필수 — `--p1`은 스킨과 무관하게 다른 데서 쓸 수 있음).
- [ ] **Step 6: 검증** — lint/build. dev 서버에서 대시보드(행성), 마이페이지(정거장), 하루 전체 완료(로켓 — 콘솔에서 `showRocket` 트리거 어려우면 태스크 전부 완료로 재현) 기본 스프라이트 표시 확인.
- [ ] **Step 7: Commit** — `Feat: 행성·완료·정거장을 스프라이트 레지스트리 기반으로 교체`

---

### Task 8: 상점 페이지 + 진입점

**Files:**
- Rewrite: `frontend/src/pages/ShopPage.jsx`
- Modify: `frontend/src/components/layout/Sidebar.jsx`(MENU), `layout/Header.jsx`(NAV_ITEMS + 코인 툴팁 문구), `frontend/src/components/SettingsModal.jsx`(구매 아이템 표시)

**Interfaces:**
- Consumes: `GET /shop/catalog`, `POST /shop/purchase`, `PUT /shop/equip`, `DELETE /shop/equip/{slot}` (Task 2), `refreshCoins()`(AuthContext — /auth/me 재조회로 equipments·코인 동시 갱신 → applySkins 자동 재적용), `PLANET_SPRITES` 등 registry (Task 7)
- Produces: 완성된 `/shop` 페이지, 사이드바·상단바 '상점' 메뉴. 구매/장착 후 `window.dispatchEvent(new CustomEvent('dumpit:catalog-updated'))` 발행 (Task 9의 스티커 피커 캐시 무효화용)

- [ ] **Step 1: 진입점** — `Sidebar.jsx` MENU와 `Header.jsx` NAV_ITEMS의 '마이페이지' **앞**에 `{ label: '상점', path: '/shop' }` 추가. Header 코인 배지 툴팁 문구를 `코인샵에서 테마와 스티커로 교환할 수 있어요.`로 교체.
- [ ] **Step 2: ShopPage 구현** — 구조:

```jsx
// 상단: 제목 + 보유 코인(coin_image.png + 잔액)
// 슬롯 섹션 순서: BACKGROUND, CHROME, POMODORO, PLANET, CELEBRATION, STATION, 스티커
// 데이터: 마운트 시 GET /shop/catalog → { coinBalance, items }
// 아이템 카드:
//  - 미리보기: THEME(BACKGROUND/CHROME/POMODORO)=팔레트 스와치(아래 SKIN_PREVIEWS), 그 외=registry 스프라이트
//  - 이름 + tier 배지(COLOR=칩 스타일, CONCEPT=starlight 강조; STICKER는 배지 없음) + 가격(coin_image)
//  - 버튼: !owned → [구매] (클릭 시 확인 모달: "N코인으로 구매할까요? 구매 후 잔액 M")
//          owned && THEME && !equipped → [장착하기]
//          owned && THEME && equipped → [장착중](disabled) + [기본으로] 버튼(슬롯 unequip)
//          owned && STICKER → [보유중](disabled)
// 액션 후: 카탈로그 재조회 + refreshCoins() + dumpit:catalog-updated 발행
// 에러: 400 응답 message를 토스트/인라인으로 표시 (기존 페이지들의 에러 표시 패턴 재사용)
```

색상 테마 미리보기 스와치는 ShopPage 내 상수로:

```js
const SKIN_PREVIEWS = { // code → 대표색 3개 (라이트 기준: bg/accent/chip 또는 chrome-bg/line, pomo focus/break)
  'bg.ocean': ['#E4EFEC', '#2E7D8A', '#D3E6E1'],
  'bg.lavender': ['#EEEAF4', '#8A63C4', '#E2DAEC'],
  'bg.sprout': ['#EAF2E3', '#5C8A3C', '#DCEBCE'],
  'chrome.mint': ['#E9F4F0', '#BFDCD3'], 'chrome.rose': ['#F6EAEA', '#E3C4C4'], 'chrome.wood': ['#F1E5D2', '#D6BE97'],
  'pomo.teal': ['#3E8E85', '#D98E2B'], 'pomo.sunset': ['#E0703C', '#8A63C4'], 'pomo.candy': ['#E05C8A', '#5CA8E0'],
}
```

스타일은 기존 `card-retro`/`btn-retro`/`chip-retro` 재사용, 그리드 `grid gap-3 sm:grid-cols-2 lg:grid-cols-3`.
- [ ] **Step 3: SettingsModal** — `/shop/items` 호출을 `/shop/catalog`로 교체, `res.data.items.filter(i => i.owned)`로 보유 목록 표시(이름만 나열해도 됨), "코인샵에서 구매해보세요!" 문구는 `/shop` 링크로.
- [ ] **Step 4: 검증** — lint/build. dev 서버: 구매 → 코인 차감·즉시 테마 반영(refreshCoins→applySkins 경유), 장착 전환, 기본으로 복귀, 잔액 부족 에러 표시.
- [ ] **Step 5: Commit** — `Feat: 상점 페이지와 사이드바·상단바 진입점 추가`

---

### Task 9: 스티커 UI (태스크·아이디어 배지 + 피커)

**Files:**
- Create: `frontend/src/components/StickerPicker.jsx`
- Modify: `frontend/src/components/dashboard/TaskListCard.jsx` (TaskRow), `frontend/src/pages/IdeaDumpPage.jsx` (아이디어 카드)

**Interfaces:**
- Consumes: `STICKER_SPRITES`/`spriteFor` (Task 7), `PUT /tasks/{id}/sticker`·`PUT /ideas/{id}/sticker` (Task 3), task/idea 응답의 `stickerCode`, `dumpit:catalog-updated` 이벤트 (Task 8)
- Produces: `<StickerPicker current={stickerCode} onSelect={(codeOrNull) => …} />` — 보유 스티커만 노출

- [ ] **Step 1: StickerPicker 작성**

```jsx
// 모듈 레벨 캐시: GET /shop/catalog → type==='STICKER' && owned 목록.
// 'dumpit:catalog-updated' 수신 시 캐시 무효화.
// UI: 트리거 버튼(현재 스티커 스프라이트 또는 '+' 점선 원형) → 팝오버(card-retro):
//   보유 스티커 스프라이트 격자(클릭=onSelect(code), 현재 선택은 테두리 강조)
//   + [떼기] (current 있을 때만, onSelect(null))
//   + 보유 0개면 "상점에서 스티커를 구매해보세요" + /shop 링크
// 외부 클릭 닫기 useEffect는 DeadlineNudgeMenu 패턴 재사용
```

- [ ] **Step 2: TaskRow 통합** — 제목 옆에 부착된 스티커 스프라이트(16px, `image-rendering:pixelated`) 표시. StickerPicker 트리거는 행의 우측 액션 영역에 배치(모바일 터치 고려해 상시 노출, 아이콘만). onSelect → `PUT /tasks/{taskId}/sticker` 낙관적 업데이트(실패 시 롤백) — DashboardPage의 기존 낙관적 업데이트 패턴(SECTION_KEYS spread)을 따르고, 완료 토글과 이벤트 충돌 없게 `stopPropagation`.
- [ ] **Step 3: 아이디어 카드 통합** — IdeaDumpPage 카드 제목 옆 배지 + 카드 액션 영역에 피커. `PUT /ideas/{ideaId}/sticker`.
- [ ] **Step 4: 검증** — lint/build. dev 서버: 부착→새로고침 후 유지(서버 저장), 떼기, 미보유 상태 안내 문구, 대시보드·아이디어 페이지 양쪽.
- [ ] **Step 5: Commit** — `Feat: 태스크·아이디어 스티커 부착 UI 추가`

---

### Task 10: 통합 검증·마무리

**Files:** (수정 없음 — 검증·기록만. QA에서 발견된 결함 수정은 이 태스크에서 커밋)

- [ ] **Step 1: 전체 게이트** — 백엔드 `./gradlew test` 전부 PASS, 프론트 `npm run lint`(0 errors)·`npm run build` 성공.
- [ ] **Step 2: 육안 QA 체크리스트** (dev 서버 + 로컬 백엔드):
  - 스킨 미장착 상태가 개편 전과 동일(기본 테마 회귀 없음) — 라이트/다크 모두
  - bg 3종 × 라이트/다크 = 6조합 가독성(텍스트 대비, 카드 그림자)
  - chrome/pomo 스킨과 bg 스킨 동시 장착 시 우선순위(chrome이 이김)
  - 구매 → 잔액 차감 + 즉시 적용, 다른 탭/기기(시크릿 창)에서 재로그인 시 동일 테마
  - 완료 애니메이션: 태스크 전체 완료로 스프라이트 발사 확인
  - 스티커: 부착·해제·새로고침 유지, 미보유 안내
  - 로그아웃 → 기본 테마 복귀, 재로그인 → 스킨 복원
- [ ] **Step 3: 팔레트 미세 조정** — QA에서 대비 부족·이질감 발견 시 index.css 스킨 값만 조정(구조 변경 금지).
- [ ] **Step 4: 문서** — `docs/superpowers/specs/2026-07-12-coin-shop-design.md`에 최종 카탈로그(이름 확정본) 반영 여부 확인, 변경 있으면 갱신 커밋.
- [ ] **Step 5: Commit** — `Chore: 상점 QA 마감(팔레트 조정·문서 갱신)` (변경분 있을 때만)

---

## 배포 시점 주의 (이번 사이클 아님 — main 푸시 때)

- prod DB `user_purchases` 실구매 0건 확인 후 배포 (V3가 전체 삭제함).
- 배포 후 확인 목록(memory의 기존 항목)에 추가: 상점 구매·장착 스모크 테스트.

## Self-Review 결과

- 스펙 대비: 슬롯 6종+스티커, A안 카탈로그(ShopCatalog 단일화), V3 가드 마이그레이션, 라이트+다크 2벌, 진입점(사이드바+상단바), 자동 장착, /auth/me equipments, localStorage 캐시, CC0 에셋, SettingsModal 갱신 — 전부 태스크에 매핑됨.
- 스펙과 다른 점 1건: 스티커 부착을 "기존 수정 API 필드"가 아닌 **전용 서브리소스 엔드포인트**(`PUT /{id}/sticker`)로 구현 — Map 기반 PATCH(태스크)와 record 기반 PATCH(아이디어)에 각각 끼워 넣는 것보다 검증 로직이 한 곳에 모여 깔끔함. 동작 요구사항은 동일.
- 타입 일관성: `equipments` 맵 키는 슬롯 enum 이름(`BACKGROUND` 등), 값은 전체 코드(`bg.ocean`) — applySkins가 접미사만 dataset에 사용, registry는 전체 코드를 키로 사용. CatalogItem.slot은 문자열(null 허용), unequip 경로변수는 enum 바인딩. 확인 완료.
